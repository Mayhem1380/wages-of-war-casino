name: Deploy Frontend to GitHub Pages
# Disabled: duplicate publish path. The active deployment is the SCP-based workflow in deploy.yml.
on:
  workflow_dispatch: {}

jobs:
  build-and-deploy:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is intentionally disabled. Use .github/workflows/deploy.yml instead."
